Install deps: `npm install`. Dev server: `npm run dev`. Production build + serve: `npm run build && npm run start`. Typecheck: `npm run typecheck`. Jest tests: `npm run test`. Playwright (per-file recommended): `npx playwright test tests/roomMachine.spec.ts` etc. Firebase Functions/hosting rely on `firebase.json`; use `npm run lint` if configured (check package scripts).